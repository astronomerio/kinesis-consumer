machine:
  services:
    - docker
  node:
    version: 7.4.0

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: astronomerio
    commands:
      - ./npm-login.sh
      - ./ci-build-release.sh
  latest:
    branch: master
    owner: astronomerio
    commands:
      - ./ci-build-latest.sh
